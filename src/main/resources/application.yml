server:
  port: 8080
spring:
  jackson:
    default-property-inclusion: NON_NULL
  jpa:
    database: mysql
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:mysql://${MYSQL_IP}:${MYSQL_PORT}/javeriana
    username: ${MYSQL_USER}
    password: ${MYSQL_PWD}
    driver-class-name: com.mysql.cj.jdbc.Driver
api:
  internal:
    jwt: 72fdc6f2-fe7f-44c0-8796-8808a39c8b3d